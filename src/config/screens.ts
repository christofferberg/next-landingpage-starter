/**
 * Default viewport sizes used across the project,
 * loaded into the Tailwind config and custom viewport hooks
 *
 * @link {useMedia} - Custom hook
 * @link {tailwind.config.js} - Tailwind configuration
 */
export const screens = {
  sm: '640px',
  md: '768px',
  lg: '1024px',
  xl: '1280px',
  '2xl': '1536px',
}
